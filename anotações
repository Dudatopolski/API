ctrl c para parar o servidor e poder voltar a digitar
instalar nodemom

a aba scripts serve para colocr comandos de atalho

post cadastrar 
catch cadastrar
putch patch atualizar 
delete excluir

request= os dados enviados do front para o back 

response = é a resposta que será devolvida para o front 

usertable = é a variável que representa a tabela de usuários no banco 

findby = vai servir como um select do mysql 